<?xml version="1.0" encoding="utf-8"?>
<!--
  30 min is the shortest period the OS honours. The app also pushes an update
  whenever study data changes, so placed widgets normally update well before
  this timer fires.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="160dp"
    android:minHeight="110dp"
    android:minResizeWidth="140dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/aura_study_widget"
    android:previewLayout="@layout/aura_study_widget"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_study_description" />
